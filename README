References used:-
1.) OSDev Wiki - http://wiki.osdev.org/Main_Page
2.) Stackoverflow - https://stackoverflow.com/
3.) AskUbuntu - https://askubuntu.com/

Note:
vma_map_iter, vma_stack, vma_heap needs to set during copy process as well for fork


Known Bugs
1.) calling fork second time with cow causes reboot, cleanup also cleans parent pages
2.) TODO: in getcwd() check if directory exists before returning anything
3.) Handle .. and . in chdir()
4.) Investigate segfault bug on exit from fork+exec and page fault handler